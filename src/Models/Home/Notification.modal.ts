import {ImageSourcePropType} from 'react-native';

export interface NotificationModal {
  id: number;
  image: string;
  name: string;
  date: string;
  title: string;
  type: string;
  video: string;
}
export interface FollowingModal {
  id: number;
  title: string;
  image: string;
  name: string;
}
